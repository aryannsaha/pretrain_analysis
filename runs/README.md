# Runs

Training outputs are intentionally ignored by Git.

- `uma/`: FAIR-Chem/UMA run directories.
- `upet/`: metatrain/UPET run directories.
- `mace/`: MACE run directories.

Use one subdirectory per experiment, for example `runs/uma/uma_sm_lr1e-4/`.
