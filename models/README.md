# Models

Large model files are intentionally ignored by Git.

- `pretrained/`: base checkpoints downloaded on the login node.
- `checkpoints/`: fine-tuned checkpoints worth keeping.
- `external_repos/`: editable clones for patched packages such as `submitit`, `metatrain`, or MACE forks.
